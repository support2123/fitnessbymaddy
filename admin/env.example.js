// Copy to /admin/env.js (gitignored) on deploy. Both values are public-safe
// (Supabase anon key + URL). Service-role key MUST NOT live here.
window.SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';
