{
  "name": "fitnessbymaddy",
  "version": "1.0.0",
  "description": "FitnessByMaddy - Vercel serverless + Supabase automation",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod",
    "db:push": "psql $DATABASE_URL -f sql/schema.sql"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "@anthropic-ai/sdk": "^0.39.0",
    "resend": "^4.1.2",
    "pdfkit": "^0.16.0"
  },
  "devDependencies": {
    "vercel": "^41.4.1"
  }
}
