{
  "name": "fitnessbymaddy-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous WhatsApp coaching pipeline for FitnessByMaddy — leads, check-ins, weekly program generation.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod --yes"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "pdfkit": "^0.15.0",
    "resend": "^4.0.0"
  }
}
