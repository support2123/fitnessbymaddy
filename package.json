{
  "name": "fitnessbymaddy",
  "version": "1.0.0",
  "private": true,
  "description": "Fitness coaching automation pipeline — lead capture, program generation, check-in tracking",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step'",
    "lint": "echo 'No linter configured yet'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@supabase/supabase-js": "^2.49.4",
    "pdfkit": "^0.16.2",
    "resend": "^4.1.2",
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
