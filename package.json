{
  "name": "fitnessbymaddy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site + serverless functions'",
    "db:migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@supabase/supabase-js": "^2.49.0",
    "pdfkit": "^0.16.0",
    "resend": "^4.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
