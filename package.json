{
  "name": "fitnessbymaddy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FitnessByMaddy site + automation pipeline (WhatsApp → Supabase → Claude → PDF).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod --yes"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "pdfkit": "^0.15.0"
  }
}
