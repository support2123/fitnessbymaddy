{
  "name": "fitnessbymaddy",
  "version": "1.0.0",
  "private": true,
  "description": "FitnessByMaddy site + WhatsApp coaching automation pipeline",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod --yes"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "pdfkit": "^0.15.0",
    "resend": "^4.0.0"
  }
}
